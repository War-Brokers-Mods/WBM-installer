export enum ROUTES {
	HOME = "#/",
	STATUS = "#/status",
	INSTALL = "#/install",
	UPDATE = "#/update",
}

// https://tauri.studio/en/docs/usage/guides/command
export enum COMMANDS {
	STATUS = "status",
	INSTALL = "install",
	UPDATE = "update",
}
